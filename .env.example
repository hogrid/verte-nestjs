# ===========================================
# APPLICATION
# ===========================================
NODE_ENV=development
PORT=3000

# CORS - Allowed origins (comma-separated)
# Production: Use your frontend domain
# Development: Use localhost URLs
CORS_ORIGIN=http://localhost:3001,http://localhost:8080

# ===========================================
# DATABASE (CRÍTICO: MESMO BANCO DO LARAVEL!)
# ===========================================
# IMPORTANTE: Use o MESMO banco de dados do Laravel
# NÃO criar novas tabelas! Compartilhar schema existente
DB_HOST=localhost
DB_PORT=5306
DB_DATABASE=VerteApp  # MESMO nome do Laravel
DB_USERNAME=root
DB_PASSWORD=root

# ===========================================
# JWT (Compatível com Laravel Sanctum)
# ===========================================
# Gere uma chave segura: openssl rand -base64 32
JWT_SECRET=your-secret-key-here-change-in-production
JWT_EXPIRATION=3600  # 1 hora em segundos

# ===========================================
# REDIS (Obrigatório para Bull Queue)
# ===========================================
# Redis é usado para filas de campanhas e jobs assíncronos
REDIS_HOST=localhost
REDIS_PORT=6379
REDIS_PASSWORD=  # deixe vazio se não tiver senha
REDIS_DB=0

# ===========================================
# WAHA - WhatsApp HTTP API
# ===========================================
# Servidor WAHA para integração WhatsApp
# Development: http://localhost:8080
# Production: https://seu-waha-server.com
WAHA_URL=http://localhost:8080
API_WHATSAPP_GLOBALKEY=your-global-key-here

# ===========================================
# STRIPE (Pagamentos)
# ===========================================
# Development: use sk_test_... e whsec_test_...
# Production: use sk_live_... e whsec_live_...
STRIPE_SECRET_KEY=sk_test_your_stripe_secret_key
STRIPE_WEBHOOK_SECRET=whsec_your_webhook_secret

# ===========================================
# MERCADOPAGO (Opcional - Brasil)
# ===========================================
MERCADOPAGO_ACCESS_TOKEN=APP_USR_your_access_token
MERCADOPAGO_PUBLIC_KEY=APP_USR_your_public_key

# ===========================================
# EMAIL (SMTP)
# ===========================================
# Gmail: Use app password (não a senha normal)
# Outros: Configure SMTP do seu provedor
MAIL_MAILER=smtp
MAIL_HOST=smtp.gmail.com
MAIL_PORT=587
MAIL_USERNAME=your-email@gmail.com
MAIL_PASSWORD=your-app-password
MAIL_ENCRYPTION=tls
MAIL_FROM_ADDRESS=noreply@verte.com
MAIL_FROM_NAME=Verte

# ===========================================
# AWS S3 (Opcional - File Storage)
# ===========================================
# Deixe vazio para usar file system local
AWS_ACCESS_KEY_ID=
AWS_SECRET_ACCESS_KEY=
AWS_DEFAULT_REGION=us-east-1
AWS_BUCKET=

# ===========================================
# PUSHER (Opcional - WebSockets)
# ===========================================
PUSHER_APP_ID=
PUSHER_APP_KEY=
PUSHER_APP_SECRET=
PUSHER_APP_CLUSTER=mt1
